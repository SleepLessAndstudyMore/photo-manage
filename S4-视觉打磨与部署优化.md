# 本地部署智能照片管理系统 — S4: 视觉打磨与部署优化

| 字段 | 内容 |
|------|------|
| **文档编号** | S4（Phase 4: 视觉打磨与部署优化） |
| **文档版本** | v3.0 |
| **编制日期** | 2026-05-26 |
| **前置条件** | S0 + S1 + S2 + S3 已完成：全部功能已实现，系统功能完整可用 |
| **AI 实现指引** | 本文档定义 Phase 4 的完整规格。AI 应基于 S1-S3 已实现的功能，将 UI 从"功能可用"提升至"视觉精致"，同时优化性能和部署健壮性。本阶段的核心产出是一个视觉品质达到 visionOS 水准、性能达标、部署稳定的完整产品 |

---

## 目录

- [1. 本阶段概述](#1-本阶段概述)
- [2. 视觉设计完整实现](#2-视觉设计完整实现)
- [3. 性能优化](#3-性能优化)
- [4. 部署优化](#4-部署优化)
- [5. 最终验收标准](#5-最终验收标准)

---

## 1. 本阶段概述

### 1.1 目标与范围

本阶段是最终收尾阶段，目标是：

1. **视觉打磨**：将 S0 定义的设计令牌完整落实到所有页面和组件，实现 visionOS 风格的极简主义视觉体验
2. **性能优化**：确保大规模照片（10 万+）下系统流畅运行
3. **部署健壮性**：启动脚本在各种环境下稳定运行
4. **边界情况处理**：完善错误处理、空状态、加载状态等

### 1.2 已有基础设施（S0 + S1 + S2 + S3 交付）

- 全部功能已实现（浏览、标签、搜索、去重、地图、人脸聚类、语义搜索）
- 设计令牌 CSS 变量已在 S0 创建（但未完整落实到所有组件）
- 启动脚本基本可用（但缺少健壮的错误处理）

---

## 2. 视觉设计完整实现

### 2.1 核心风格定义

以苹果设计语言为基础，融合 visionOS 的半透明质感与空间感。核心词：**留白、毛玻璃、悬浮感、微光影**。

### 2.2 色彩与材质完整实现

**浅色模式：**

```css
:root {
  /* 底色 */
  --bg-primary: #FFFFFF;
  --bg-secondary: #F8F8F8;
  --bg-tertiary: #F0F0F0;

  /* 边框 */
  --border-color: #F0F0F0;
  --border-color-hover: #E0E0E0;

  /* 文字 */
  --text-primary: #1D1D1F;
  --text-secondary: #86868B;
  --text-tertiary: #AEAEB2;

  /* 强调色 — 柔和蒂芙尼蓝 */
  --accent: #7EC8C8;
  --accent-hover: #6BB8B8;
  --accent-light: rgba(126, 200, 200, 0.12);

  /* 毛玻璃卡片 */
  --card-bg: rgba(255, 255, 255, 0.72);
  --card-blur: blur(40px);
  --card-shadow: 0 2px 16px rgba(0, 0, 0, 0.06);
  --card-shadow-hover: 0 8px 32px rgba(0, 0, 0, 0.10);

  /* 渐变光晕 */
  --bg-glow: radial-gradient(ellipse at 20% 50%, rgba(126, 200, 200, 0.08) 0%, transparent 50%),
             radial-gradient(ellipse at 80% 20%, rgba(200, 180, 255, 0.06) 0%, transparent 50%);
}
```

**深色模式：**

```css
:root[data-theme="dark"] {
  /* 底色 */
  --bg-primary: #1E1E1E;
  --bg-secondary: #2A2A2A;
  --bg-tertiary: #333333;

  /* 边框 */
  --border-color: #3A3A3A;
  --border-color-hover: #4A4A4A;

  /* 文字 */
  --text-primary: #F5F5F7;
  --text-secondary: #A1A1A6;
  --text-tertiary: #6E6E73;

  /* 强调色 */
  --accent: #7EC8C8;
  --accent-hover: #92D6D6;
  --accent-light: rgba(126, 200, 200, 0.15);

  /* 毛玻璃卡片 — 模拟发光体光影溢出 */
  --card-bg: rgba(30, 30, 30, 0.7);
  --card-blur: blur(60px);
  --card-shadow: 0 2px 16px rgba(0, 0, 0, 0.3);
  --card-shadow-hover: 0 8px 32px rgba(0, 0, 0, 0.4);

  /* 渐变光晕 — 深色下更明显 */
  --bg-glow: radial-gradient(ellipse at 20% 50%, rgba(126, 200, 200, 0.06) 0%, transparent 50%),
             radial-gradient(ellipse at 80% 20%, rgba(200, 180, 255, 0.04) 0%, transparent 50%);
}
```

### 2.3 排版与间距

**字体族：**

```css
:root {
  --font-sans: -apple-system, "PingFang SC", "Microsoft YaHei", "Helvetica Neue", Arial, sans-serif;
  --font-display: "SF Pro Display", "PingFang SC", "Microsoft YaHei", -apple-system, sans-serif;
  --font-mono: "SF Mono", "Fira Code", "Consolas", monospace;
}
```

**字号层级：**

```css
:root {
  --text-xs: 11px;
  --text-sm: 13px;
  --text-base: 15px;
  --text-lg: 17px;
  --text-xl: 20px;
  --text-2xl: 24px;
  --text-3xl: 32px;
  --text-4xl: 40px;
}
```

**间距系统（大呼吸感）：**

```css
:root {
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
  --space-2xl: 48px;
  --space-3xl: 64px;
}
```

页面两侧留白至少 `--space-xl`（32px），网格间距至少 `--space-md`（16px）至 `--space-lg`（24px），绝不允许内容挤满屏幕。

### 2.4 照片展示特效

**悬浮卡片：**

照片缩略图不再是贴在白板上的方块，而是带有轻微阴影的悬浮卡片，鼠标 Hover 时轻微放大并加深阴影，产生"拾起"的物理反馈：

```css
.photo-card {
  border-radius: 8px;
  overflow: hidden;
  box-shadow: var(--card-shadow);
  transition: transform 0.25s cubic-bezier(0.4, 0, 0.2, 1),
              box-shadow 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  cursor: pointer;
}

.photo-card:hover {
  transform: scale(1.02);
  box-shadow: var(--card-shadow-hover);
}
```

**沉浸式大图（照片详情页）：**

照片详情页背景自动模糊提取照片主色调，照片本体悬浮在毛玻璃背景之上，营造剧场感：

```css
.photo-detail-backdrop {
  position: fixed;
  inset: 0;
  background-size: cover;
  background-position: center;
  filter: blur(80px) brightness(0.6);
  transform: scale(1.2); /* 避免模糊边缘露出 */
  z-index: -1;
}

.photo-detail-image {
  max-width: 90vw;
  max-height: 85vh;
  object-fit: contain;
  border-radius: 4px;
  box-shadow: 0 16px 64px rgba(0, 0, 0, 0.4);
}
```

### 2.5 组件风格

**按钮 — 极简胶囊按钮：**

```css
.btn {
  display: inline-flex;
  align-items: center;
  gap: var(--space-sm);
  padding: 8px 20px;
  border-radius: 100px;
  border: none;
  background: transparent;
  color: var(--text-primary);
  font-size: var(--text-sm);
  font-weight: 500;
  cursor: pointer;
  transition: background 0.2s ease;
}

.btn:hover {
  background: var(--accent-light);
}

.btn-primary {
  background: var(--accent);
  color: white;
}

.btn-primary:hover {
  background: var(--accent-hover);
}
```

**弹窗/抽屉 — 右侧毛玻璃抽屉：**

右侧抽屉式面板（查看 EXIF/标签），带毛玻璃背景，滑入丝滑。抽屉宽度 360px-420px：

```css
.drawer-panel {
  position: fixed;
  top: 0;
  right: 0;
  width: 400px;
  height: 100vh;
  backdrop-filter: blur(40px);
  background: rgba(255, 255, 255, 0.85);
  border-left: 1px solid var(--border-color);
  transform: translateX(100%);
  transition: transform 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  z-index: 1000;
}

.drawer-panel.open {
  transform: translateX(0);
}
```

**滚动条 — 全局隐藏，Hover 显现：**

```css
/* 全局隐藏原生滚动条 */
::-webkit-scrollbar {
  width: 6px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: transparent;
  border-radius: 3px;
}

/* Hover 时显现极细半透明滚动条 */
:hover::-webkit-scrollbar-thumb {
  background: rgba(0, 0, 0, 0.2);
}

:hover::-webkit-scrollbar-thumb:hover {
  background: rgba(0, 0, 0, 0.4);
}

/* 深色模式 */
[data-theme="dark"] :hover::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.2);
}

[data-theme="dark"] :hover::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, 0.4);
}
```

### 2.6 深色模式切换

- 系统默认跟随操作系统主题（`prefers-color-scheme`）
- 用户可在设置页手动切换浅色/深色模式
- 切换时全局平滑过渡（`transition: background 0.3s, color 0.3s`）
- 用户选择持久化到 localStorage

---

## 3. 性能优化

### 3.1 大规模照片优化

| 优化项 | 目标 | 实现方式 |
|--------|------|----------|
| 时间轴加载 | 10 万张照片首页加载 < 1s | 后端按年/月预聚合，前端仅加载层级数据而非全量照片 |
| 缩略图瀑布流 | 1 万张照片虚拟滚动无卡顿 | vue-virtual-scroller + 预计算占位尺寸 + 懒加载 |
| 搜索响应 | 10 万级照片搜索 < 600ms | sqlite-vss ANN 索引 + 结构化过滤前置 |
| 缩略图生成 | 1000 张/分钟（CPU） | 多进程并行 + ffmpeg GPU 加速（可选） |
| 数据库体积 | 10 万张照片数据库 < 500MB | 向量数据独立表 + 定期 VACUUM |

### 3.2 前端加载优化

- 路由懒加载（所有页面组件 `() => import(...)`）
- 缩略图使用 WebP 格式（体积减少 30%）
- 大图预览使用渐进式 JPEG
- 关键 CSS 内联，非关键 CSS 异步加载

### 3.3 后端性能优化

- API 响应启用 gzip 压缩
- 缩略图路径支持浏览器缓存（ETag / Last-Modified）
- 数据库查询使用索引覆盖（避免 SELECT *）
- CLIP 向量生成使用多进程并行

---

## 4. 部署优化

### 4.1 启动脚本健壮性

在 S0 基础启动脚本上增加以下健壮性处理：

**错误检测与友好提示：**

- Python 版本检测（必须 3.10+，否则提示下载链接）
- venv 创建失败处理（权限问题、路径含中文等）
- pip 安装失败重试（网络超时自动重试 3 次，切换镜像源）
- 端口占用检测（8000 端口被占用时自动选择下一个可用端口）
- 模型权重下载失败重试（支持断点续传）

**离线依赖包支持：**

- 提供 `requirements_cache/` 目录，预存所有 pip 依赖的 wheel 文件
- 启动脚本优先从本地缓存安装，缓存不存在时才从网络下载
- 方便无网络环境下部署

### 4.2 跨平台兼容性

- Windows：路径含空格/中文的处理；`.bat` 脚本编码 UTF-8（chcp 65001）
- macOS：`.command` 脚本执行权限自动设置；Apple Silicon (M1/M2) 原生支持
- ffmpeg 路径：自动检测系统已安装的 ffmpeg，未安装时提示安装

### 4.3 边界情况处理

| 场景 | 处理方式 |
|------|----------|
| 空图库源 | 显示友好的空状态提示（插图 + "添加你的第一张照片"引导） |
| 损坏的照片文件 | 缩略图生成失败时显示占位图 + "文件损坏"标签，不阻塞扫描 |
| 超大照片（>100MB） | 缩略图生成超时保护（30s），超时显示占位图 |
| 数据库损坏 | 启动时检测数据库完整性，损坏时提示用户从备份恢复或重建索引 |
| 磁盘空间不足 | 缩略图生成前检测剩余空间，不足时暂停并提示 |
| 并发操作冲突 | 乐观锁 + 最后写入胜出，关键操作（删除）使用悲观锁 |

---

## 5. 最终验收标准

**全部功能验收（S1 + S2 + S3 + S4）：**

### 核心浏览（S1）

1. 可添加图库源并完成目录扫描，原文件无任何变动
2. 时间轴按年/月/日层级正确展示照片
3. 缩略图异步生成（含视频抽帧），前端加载流畅
4. EXIF 元数据正确解析和展示
5. 虚拟滚动在 1 万张照片下流畅运行，瀑布流无布局抖动
6. 文件夹视图和聚合视图可正常切换
7. 一键启动脚本双击即可启动并自动打开浏览器
8. 照片方向根据 EXIF Orientation 正确显示
9. 文件一致性校验能正确标记丢失文件和发现新增文件

### 智能整理（S2）

10. 标签和相册功能完整可用，YOLOv8 标签附带置信度
11. 高级搜索返回正确结果，混合搜索正确提取时间实体
12. 重复照片清理区分比特级和视觉级
13. 地图视图正确展示 GPS 照片
14. 中文搜索"猫"能正确命中标签为"cat"的照片

### AI 赋能（S3）

15. CLIP 自然语言搜索返回语义相关结果
16. 人脸聚类准确率 ≥ 85%

### 视觉与部署（S4）

17. 所有页面符合 visionOS 极简主义视觉规范（毛玻璃、悬浮卡片、胶囊按钮、极细滚动条）
18. 深色模式完整可用，切换平滑
19. 照片详情页沉浸式大图效果（背景模糊 + 主色调提取）
20. 10 万张照片下系统流畅运行（首页加载 < 1s，搜索 < 600ms）
21. 启动脚本在 Windows/macOS 上稳定运行，错误提示友好
22. 空状态、加载状态、错误状态均有友好的 UI 提示
23. 物理删除操作正确移入系统回收站（send2trash）
