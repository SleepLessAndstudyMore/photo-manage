# 本地部署智能照片管理系统 — S0: 项目脚手架与共享规范

| 字段 | 内容 |
|------|------|
| **项目名称** | 本地部署智能照片管理系统 |
| **文档编号** | S0（基础规范） |
| **文档版本** | v3.0 |
| **编制日期** | 2026-05-26 |
| **文档性质** | 全局共享规范，所有后续子文档（S1-S4）均依赖本文档 |
| **AI 实现指引** | 本文档定义项目的技术选型、数据模型、API 规范、视觉设计令牌和部署架构。AI 应首先基于本文档完成项目脚手架搭建，包括：创建项目目录结构、初始化数据库 Schema、搭建 FastAPI 后端骨架和 Vue 前端骨架、配置启动脚本 |

---

## 目录

- [1. 项目概述](#1-项目概述)
- [2. 技术架构](#2-技术架构)
- [3. 完整数据模型](#3-完整数据模型)
- [4. API 通用规范](#4-api-通用规范)
- [5. 视觉设计规范（设计令牌）](#5-视觉设计规范设计令牌)
- [6. 部署与工程化](#6-部署与工程化)
- [7. 非功能规格摘要](#7-非功能规格摘要)
- [8. 项目目录结构](#8-项目目录结构)

---

## 1. 项目概述

### 1.1 项目背景

当前用户拥有大量照片，以"日期（天）文件夹"的形式存储在本地硬盘中（如 `2023-10-25`），这种纯文件系统层级的组织方式存在以下痛点：

- **浏览效率低下**：用户需要逐层打开文件夹才能查看照片，无法快速定位到特定时间段的照片集合，缺乏全局视角的浏览体验。
- **无法按内容检索**：文件系统仅提供基于文件名和路径的检索能力，无法根据照片内容（如"猫"、"海滩"、"建筑"）进行语义化搜索，导致大量时间浪费在手动翻找上。
- **难以去重和整理**：连拍、重复导入等场景产生大量相似或重复照片，纯文件系统无法自动识别和提示，用户只能逐张人工比对，效率极低。
- **缺乏元数据利用**：照片中嵌入的 EXIF 信息（拍摄时间、相机参数、GPS 位置等）未被提取和利用，无法基于这些维度进行筛选和展示。
- **隐私顾虑**：现有云端照片管理服务（如 Google Photos、iCloud）需要将照片上传至服务器，存在隐私泄露风险，不符合用户对数据完全自主可控的需求。

### 1.2 核心目标与设计原则

开发一个**本地部署的 Web 端照片管理系统**，实现以下核心目标：

1. **无损接入**：能够直接映射现有日期目录结构，不移动、不修改原始照片文件，保障数据完整性。
2. **智能分类**：通过提取 EXIF 元数据和本地 AI 视觉识别，实现照片的自动分类与标签化。
3. **快速检索**：支持基于时间、地点、标签、相机参数等多维度组合检索，以及自然语言语义搜索。
4. **可视化浏览**：提供时间轴、地图、瀑布流等多种可视化浏览方式，提升浏览效率。
5. **完全本地化**：所有数据处理、AI 推理均在本地完成，严禁调用外部云 API，保障用户隐私。

**设计原则：**

| 原则 | 说明 |
|------|------|
| **只读管理，不做编辑器** | 系统的核心职责是管理、检索、浏览，而非修图。所有对照片的"修改"仅限于数据库元数据层面的调整（如方向纠正），绝不生成新的像素文件。修图需求应交给专业工具 |
| **一键启动，原生桌面级体验** | 原生桌面级体验，双击即用，无感知后端。系统提供双击可运行的启动脚本（start.bat / start.command），首次运行自动配置环境，后续启动直接进入系统，自动打开浏览器即可使用 |
| **AI 职责解耦** | CLIP 专司语义搜索（向量化匹配），YOLOv8 专司物体标签（文本分类），两者各司其职，不交叉使用。避免用 CLIP 生成长尾文本标签的错位做法 |
| **混合搜索优先** | 自然语言搜索采用"结构化提取 + 语义匹配"的混合策略，先提取时间/相机等结构化实体作为 Filter，再对语义部分进行 CLIP 向量检索 |

### 1.3 术语与缩略语

| 术语/缩略语 | 说明 |
|-------------|------|
| EXIF | Exchangeable Image File Format，可交换图像文件格式，包含照片的拍摄参数元数据 |
| CLIP | Contrastive Language-Image Pre-Training，OpenAI 开源的图文匹配模型，**本系统中仅用于语义搜索** |
| YOLOv8 | You Only Look Once v8，实时目标检测模型，**本系统中用于生成物体标签** |
| InsightFace | 开源人脸分析与识别工具包 |
| phash | Perceptual Hash，感知哈希，用于图片视觉相似度比较 |
| file_hash | 文件内容哈希（MD5/SHA256），用于比特级精确去重 |
| Embedding | 向量嵌入，将图像/文本映射到高维向量空间 |
| MVP | Minimum Viable Product，最小可行产品 |
| 虚拟滚动 | Virtual Scrolling，仅渲染可视区域 DOM 元素的性能优化技术 |
| 混合搜索 | Hybrid Search，结构化条件过滤 + 语义向量检索的组合搜索策略 |
| 增量扫描 | Incremental Scan，仅处理新增/修改文件，跳过已索引文件的扫描策略 |
| send2trash | Python 跨平台库，将文件移至系统回收站而非永久删除 |

### 1.4 实现路线图

本项目分为 5 个子文档，按顺序实现：

| 顺序 | 文档 | 核心内容 | 预计工期 |
|------|------|----------|---------|
| **S0** | 项目脚手架与共享规范（本文档） | 技术选型、数据模型、API 规范、视觉令牌、部署架构 | 1-2 天 |
| **S1** | 核心浏览 MVP | 目录扫描、缩略图、时间轴、EXIF、文件监控 | 4-6 周 |
| **S2** | 智能整理与检索 | YOLOv8 标签、相册、混合搜索、去重、地图 | 3-5 周 |
| **S3** | AI 语义搜索与人脸聚类 | CLIP 语义搜索、InsightFace 人脸聚类 | 3-4 周 |
| **S4** | 视觉打磨与部署优化 | visionOS 完整实现、性能优化、启动脚本健壮性 | 2-3 周 |

---

## 2. 技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     用户浏览器                           │
│  Vue 3 + TypeScript + Vite + Element Plus/Naive UI      │
│  vue-virtual-scroller | PhotoSwipe | Leaflet/Mapbox     │
└────────────────────────┬────────────────────────────────┘
                         │ HTTP / WebSocket
┌────────────────────────▼────────────────────────────────┐
│                   FastAPI 后端                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────┐  │
│  │ REST API │ │ WebSocket│ │ 任务调度  │ │ AI 推理   │  │
│  │  路由层  │ │  通知层  │ │  管理层  │ │  服务层   │  │
│  └──────────┘ └──────────┘ └──────────┘ └───────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────┐  │
│  │ EXIF解析 │ │ 缩略图   │ │ 感知哈希 │ │ 混合搜索  │  │
│  │  服务    │ │  服务    │ │  服务    │ │  引擎     │  │
│  └──────────┘ └──────────┘ └──────────┘ └───────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │ 前端静态 │ │ 系统托盘 │ │ 浏览器   │               │
│  │ 资源托管 │ │ 整合模块 │ │ 自动唤起 │               │
│  └──────────┘ └──────────┘ └──────────┘               │
└──────────┬──────────────┬──────────────────────────────┘
           │              │
┌──────────▼──────┐ ┌─────▼──────────────────────────────┐
│   SQLite 数据库  │ │        本地文件系统                  │
│  (SQLModel/     │ │  ┌────────┐ ┌────────┐ ┌────────┐ │
│   SQLAlchemy)   │ │  │原图目录│ │缩略图  │ │模型权重│ │
│                 │ │  │        │ │  目录  │ │  目录  │ │
│  + sqlite-vss   │ │  └────────┘ └────────┘ └────────┘ │
│  (向量搜索扩展)  │ │                                    │
└─────────────────┘ └────────────────────────────────────┘
```

**架构设计要点：**

- **零外部中间件**：不依赖 Redis、RabbitMQ 等外部中间件。后台任务调度通过 SQLite 任务表 + Python `concurrent.futures` / `multiprocessing` 实现，确保一键启动。
- **混合搜索引擎**：自然语言搜索由"结构化实体提取 + CLIP 向量检索"两部分协同完成，而非将整段查询直接丢给 CLIP。
- **向量存储分离**：CLIP 向量嵌入存储在独立的 `PhotoEmbedding` 表中（或 sqlite-vss 虚拟表），与 Photo 主表分离，避免大 BLOB 影响常规查询性能。
- **前后端统一端口**：前端 Vite 构建产物（HTML/JS/CSS）由 FastAPI 作为静态资源托管，前后端共用同一端口，用户无需关心端口配置。
- **一键启动脚本分发**：提供双击可运行的启动脚本（start.bat / start.command），首次运行自动创建 venv 并安装依赖，无需手动配置环境。

### 2.2 后端技术栈

| 组件 | 技术选型 | 选型理由 |
|------|---------|---------|
| 语言与框架 | Python 3.11+ / FastAPI | Python 在图像处理和 AI 生态具有绝对优势；FastAPI 异步支持好，自动生成 OpenAPI 文档 |
| EXIF 解析 | `exiftool`（Python 封装）/ `piexif` | exiftool 是最全面的 EXIF 解析工具，piexif 作为轻量备选 |
| 缩略图生成 | `Pillow` + `ffmpeg-python` | Pillow 处理图片缩放，ffmpeg 抽取视频帧 |
| 感知哈希 | `ImageHash` | 成熟的感知哈希库，支持 pHash/dHash/aHash |
| 数据库 | `SQLite` + `SQLModel`/`SQLAlchemy` | 零配置嵌入式数据库，本地个人工具无需 MySQL/PostgreSQL |
| 向量搜索 | `sqlite-vss` 或 `ChromaDB` | 用于存储和检索 CLIP 生成的图像 Embedding |
| 异步任务 | **`Huey`**（基于 SQLite）或 Python `concurrent.futures` + SQLite 任务表 | **本地个人工具零外部中间件依赖**。Huey 基于 SQLite 实现任务队列，无需安装 Redis |
| 数据验证 | `Pydantic` v2 | FastAPI 原生集成，用于请求/响应模型的类型校验 |
| 文件监控 | `Watchdog`（可选） | 监控图库源目录变动，默认关闭，用户可按需开启 |
| 安全删除 | `send2trash` | 跨平台将文件移至系统回收站，支持 Windows/macOS/Linux，确保删除操作可恢复 |
| 系统托盘 | `pystray` | 在系统托盘（Taskbar/Menu Bar）显示图标，提供"打开界面"、"停止服务"等操作，无需原生打包 |

### 2.3 前端技术栈

| 组件 | 技术选型 | 选型理由 |
|------|---------|---------|
| 框架 | Vue 3 + TypeScript + Vite | Vue 3 Composition API 灵活高效，TypeScript 提供类型安全，Vite 构建速度快 |
| UI 组件库 | Element Plus 或 Naive UI | 两者均为 Vue 3 生态成熟组件库，Naive UI 更轻量，Element Plus 功能更全 |
| 虚拟滚动 | `vue-virtual-scroller` | 成熟的 Vue 3 虚拟滚动方案，支持网格和列表布局 |
| 大图预览 | `PhotoSwipe` | 专业的图片浏览库，支持手势缩放、滑动切换 |
| 地图展示 | `Leaflet` + 离线瓦片 | Leaflet 轻量开源，离线瓦片方案保障断网可用 |
| 状态管理 | `Pinia` | Vue 3 官方推荐的状态管理方案，TypeScript 支持好 |
| HTTP 客户端 | `Axios` | 拦截器机制完善，支持请求取消和进度监控 |

### 2.4 本地 AI 模型栈

| 功能 | 模型/库 | 职责边界 | 说明 |
|------|--------|---------|------|
| 物体标签生成 | YOLOv8-nano | **仅负责生成物体标签**（输出 COCO 80 类别 + 置信度） | 轻量快速，识别日常物体，nano 版本仅 6MB。输出如：`{class: "cat", confidence: 0.92}` |
| 语义搜索 | CLIP (ViT-B/32) | **仅负责语义向量检索**（将文本/图像映射到向量空间进行相似度匹配） | 不参与标签生成。模型约 600MB，可在 CPU 上运行（GPU 更快） |
| 人脸检测与聚类 | InsightFace (buffalo_l) | 负责人脸检测、特征提取、人脸比对与聚类 | 开源人脸分析工具包 |
| 向量存储 | sqlite-vss 虚拟表 或 ChromaDB | 存储 CLIP 生成的图像 Embedding，支持近似最近邻搜索（ANN） | 向量数据与 Photo 主表分离存储 |

**AI 职责解耦说明：**

```
用户上传/扫描照片
       │
       ├──→ YOLOv8 推理 ──→ 输出物体标签（cat, dog, car...）──→ 写入 Tag 表
       │                                                         type='auto'
       │
       └──→ CLIP 推理 ──→ 输出 512维向量 ──→ 写入 PhotoEmbedding 表
                                                （用于语义搜索，不生成标签）

用户搜索"去年在海边的猫"
       │
       ├──→ NLP/正则提取 ──→ 时间实体: "去年" → date_from/date_to
       │                     结构化实体: "猫" → 通过 name_zh 字段映射至 tag_id（Tag.name_zh="猫" → Tag.name="cat" → tag_id）
       │
       └──→ CLIP 文本编码 ──→ "海边" → 512维向量 ──→ 向量检索
                                                   │
                              Filter: date_range + tag_id
                                                   │
                              结果 = 向量检索 ∩ Filter
```

**模型运行环境要求：**

| 配置项 | 最低配置 | 推荐配置 |
|--------|---------|---------|
| CPU | 4 核 x86_64 | 8 核+ x86_64 |
| 内存 | 8 GB | 16 GB+ |
| GPU | 无（CPU 推理） | NVIDIA GPU（CUDA 支持），推理速度提升 5-10 倍 |
| 磁盘 | 2 GB（模型权重 + 数据库） | SSD，预留图片库 10% 的空间用于缩略图 |

---

## 3. 完整数据模型

> **重要**：以下为系统的完整数据模型。S0 阶段需创建全部表结构（即使某些表在后续阶段才使用），确保数据库 Schema 一次性到位，避免后续阶段需要做数据库迁移。

### 3.1 实体关系概览

```
LibrarySource ──1:N──→ Photo
Photo ──N:M──→ Tag (通过 PhotoTag)
Photo ──N:M──→ Album (通过 PhotoAlbum)
Photo ──1:1──→ PhotoEmbedding
Photo ──1:N──→ PhotoFace
FaceCluster ──1:N──→ PhotoFace
```

### 3.2 数据表详细定义

#### 3.2.1 LibrarySource（图库源）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PK, AUTO | 主键 |
| name | TEXT | NOT NULL | 图库源名称 |
| path | TEXT | NOT NULL, UNIQUE | 文件系统路径 |
| type | TEXT | NOT NULL | 类型：local（本地目录） |
| scan_status | TEXT | DEFAULT 'idle' | 扫描状态：idle / scanning / completed / error |
| last_scan_at | DATETIME | NULL | 上次扫描时间 |
| photo_count | INTEGER | DEFAULT 0 | 照片数量（缓存字段） |
| created_at | DATETIME | NOT NULL, DEFAULT NOW | 创建时间 |

#### 3.2.2 Photo（照片）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PK, AUTO | 主键 |
| library_source_id | INTEGER | FK → LibrarySource.id | 所属图库源 |
| file_path | TEXT | NOT NULL, UNIQUE | 文件完整路径 |
| file_name | TEXT | NOT NULL | 文件名 |
| file_size | INTEGER | NOT NULL | 文件大小（字节） |
| file_hash | TEXT | NULL | 文件内容哈希（MD5），用于比特级去重 |
| phash | TEXT | NULL | 感知哈希，用于视觉相似度比较 |
| is_video | BOOLEAN | DEFAULT FALSE | 是否为视频文件 |
| date_taken | DATETIME | NULL | 拍摄时间（EXIF DateTimeOriginal） |
| date_modified | DATETIME | NULL | 文件修改时间 |
| thumbnail_path | TEXT | NULL | 小缩略图路径 |
| thumbnail_width | INTEGER | NULL | 小缩略图实际宽度（px） |
| thumbnail_height | INTEGER | NULL | 小缩略图实际高度（px） |
| preview_path | TEXT | NULL | 中预览图路径 |
| width | INTEGER | NULL | 原图宽度 |
| height | INTEGER | NULL | 原图高度 |
| orientation | INTEGER | DEFAULT 1 | EXIF 方向值（1-8） |
| camera_make | TEXT | NULL | 相机厂商 |
| camera_model | TEXT | NULL | 相机型号 |
| lens_model | TEXT | NULL | 镜头型号 |
| f_number | REAL | NULL | 光圈值 |
| exposure_time | TEXT | NULL | 快门速度 |
| iso | INTEGER | NULL | ISO 感光度 |
| focal_length | REAL | NULL | 焦距（mm） |
| gps_latitude | REAL | NULL | GPS 纬度 |
| gps_longitude | REAL | NULL | GPS 经度 |
| rating | INTEGER | DEFAULT 0 | 评分（0-5） |
| is_favorite | BOOLEAN | DEFAULT FALSE | 是否收藏 |
| file_missing | BOOLEAN | DEFAULT FALSE | 文件是否丢失（v2.0 新增） |
| duration | REAL | NULL | 视频时长（秒），仅视频文件 |
| created_at | DATETIME | NOT NULL, DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | NOT NULL, DEFAULT NOW | 更新时间 |

**Photo 表索引：**

| 索引名 | 字段 | 类型 | 用途 |
|--------|------|------|------|
| idx_photo_date | date_taken | B-Tree | 时间轴查询 |
| idx_photo_library | library_source_id | B-Tree | 按图库源筛选 |
| idx_photo_hash | file_hash | B-Tree | 比特级去重 |
| idx_photo_phash | phash | B-Tree | 视觉相似度查询 |
| idx_photo_camera | camera_model | B-Tree | 按相机筛选 |
| idx_photo_missing | file_missing | B-Tree | 丢失文件筛选 |

#### 3.2.3 Tag（标签）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PK, AUTO | 主键 |
| name | TEXT | NOT NULL, UNIQUE | 标签名称（英文，如 "cat"、"dog"） |
| name_zh | TEXT | NULL | 标签中文名称（如 "猫"、"狗"），用于中文搜索映射与界面展示 |
| type | TEXT | NOT NULL | 标签类型：auto（YOLOv8 自动）/ manual（手动）/ face（人脸） |
| photo_count | INTEGER | DEFAULT 0 | 关联照片数量（缓存字段） |
| created_at | DATETIME | NOT NULL, DEFAULT NOW | 创建时间 |

#### 3.2.4 PhotoTag（照片-标签关联）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| photo_id | INTEGER | FK → Photo.id, PK | 照片 ID |
| tag_id | INTEGER | FK → Tag.id, PK | 标签 ID |
| confidence | REAL | NULL | AI 标签的置信度（0-1），如照片 A 识别到猫的置信度为 0.92，照片 B 为 0.71 |
| source | TEXT | NOT NULL, DEFAULT 'auto' | 标签来源：auto / manual |
| created_at | DATETIME | NOT NULL, DEFAULT NOW | 关联时间 |

#### 3.2.5 Album（相册）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PK, AUTO | 主键 |
| name | TEXT | NOT NULL | 相册名称 |
| description | TEXT | NULL | 相册描述 |
| cover_photo_id | INTEGER | FK → Photo.id, NULL | 封面照片 |
| photo_count | INTEGER | DEFAULT 0 | 照片数量（缓存字段） |
| created_at | DATETIME | NOT NULL, DEFAULT NOW | 创建时间 |
| updated_at | DATETIME | NOT NULL, DEFAULT NOW | 更新时间 |

#### 3.2.6 PhotoAlbum（照片-相册关联）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| photo_id | INTEGER | FK → Photo.id, PK | 照片 ID |
| album_id | INTEGER | FK → Album.id, PK | 相册 ID |
| created_at | DATETIME | NOT NULL, DEFAULT NOW | 关联时间 |

#### 3.2.7 FaceCluster（人脸簇）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PK, AUTO | 主键 |
| name | TEXT | NULL | 人物名称（用户可编辑） |
| representative_face_id | INTEGER | FK → PhotoFace.id, NULL | 代表人脸 |
| face_count | INTEGER | DEFAULT 0 | 人脸数量（缓存字段） |
| created_at | DATETIME | NOT NULL, DEFAULT NOW | 创建时间 |

#### 3.2.8 PhotoFace（照片-人脸关联）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PK, AUTO | 主键 |
| photo_id | INTEGER | FK → Photo.id | 照片 ID |
| face_cluster_id | INTEGER | FK → FaceCluster.id, NULL | 所属人脸簇（未聚类时为 NULL） |
| bbox_x | INTEGER | NOT NULL | 人脸边界框左上角 X |
| bbox_y | INTEGER | NOT NULL | 人脸边界框左上角 Y |
| bbox_w | INTEGER | NOT NULL | 人脸边界框宽度 |
| bbox_h | INTEGER | NOT NULL | 人脸边界框高度 |
| face_embedding | BLOB | NOT NULL | 人脸特征向量（512维 float32） |
| confidence | REAL | NOT NULL | 检测置信度 |
| thumbnail_path | TEXT | NULL | 人脸缩略图路径 |
| created_at | DATETIME | NOT NULL, DEFAULT NOW | 创建时间 |

#### 3.2.9 PhotoEmbedding（照片向量嵌入）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| photo_id | INTEGER | FK → Photo.id, PK | 照片 ID（与 Photo 一对一） |
| embedding_blob | BLOB | NOT NULL | CLIP 向量嵌入（512维 float32，约 2KB） |
| embedding_model | TEXT | NOT NULL, DEFAULT 'clip-vit-b-32' | 生成向量的模型标识 |
| created_at | DATETIME | NOT NULL, DEFAULT NOW | 创建时间 |

> **设计说明**：将 `clip_embedding` 从 Photo 主表剥离为独立表，原因如下：1）512维 float32 向量约 2KB，与主表混存会导致单行数据过大，严重影响 SQLite 对 Photo 表的扫描性能；2）向量数据仅在语义搜索时使用，常规查询（时间轴、标签浏览等）不需要加载向量；3）独立表便于未来迁移至 sqlite-vss 虚拟表或 ChromaDB，不影响主表结构。

---

## 4. API 通用规范

### 4.1 URL 模式与版本

所有 API 路径统一前缀：`/api/v1/`

### 4.2 统一响应格式

```json
// 成功响应（列表）
{
  "items": [...],
  "total": 1234,
  "page": 1,
  "page_size": 50
}

// 成功响应（单条）
{
  "id": 1,
  "name": "...",
  ...
}

// 错误响应
{
  "detail": "错误描述信息"
}
```

### 4.3 错误码定义

| HTTP 状态码 | 含义 | 使用场景 |
|------------|------|---------|
| 400 | Bad Request | 请求参数校验失败 |
| 404 | Not Found | 资源不存在 |
| 409 | Conflict | 资源冲突（如重复添加） |
| 422 | Unprocessable Entity | Pydantic 校验失败 |
| 500 | Internal Server Error | 服务端内部错误 |

### 4.4 分页规范

列表接口统一支持分页参数：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | int | 1 | 页码（从 1 开始） |
| page_size | int | 50 | 每页数量（最大 200） |

### 4.5 WebSocket 通知规范

后端通过 WebSocket 推送实时通知，路径：`ws://localhost:8000/api/v1/ws/notifications`

通知消息格式：

```json
{
  "type": "scan_progress | thumbnail_progress | tag_progress | face_progress",
  "data": {
    "library_id": 1,
    "progress": 0.65,
    "message": "正在扫描..."
  }
}
```

### 4.6 完整 API 路由清单

> **重要**：以下为系统全部 API 路由。S0 阶段需创建所有路由的骨架（返回空数据或 501 Not Implemented），后续阶段逐步填充实现。

#### 图库源管理

| 方法 | 路径 | 说明 | 实现阶段 |
|------|------|------|---------|
| GET | `/api/v1/libraries` | 获取图库源列表 | S1 |
| POST | `/api/v1/libraries` | 添加图库源 | S1 |
| DELETE | `/api/v1/libraries/{id}` | 删除图库源 | S1 |
| POST | `/api/v1/libraries/{id}/scan` | 触发目录扫描 | S1 |
| POST | `/api/v1/libraries/{id}/check-consistency` | 触发一致性校验 | S1 |

#### 照片浏览与检索

| 方法 | 路径 | 说明 | 实现阶段 |
|------|------|------|---------|
| GET | `/api/v1/photos` | 获取照片列表（分页） | S1 |
| GET | `/api/v1/photos/{id}` | 获取照片详情 | S1 |
| GET | `/api/v1/photos/{id}/exif` | 获取照片 EXIF 信息 | S1 |
| PUT | `/api/v1/photos/{id}` | 更新照片元数据（评分、收藏等） | S1 |
| POST | `/api/v1/photos/search` | 搜索照片（结构化 + 混合搜索） | S2 |
| GET | `/api/v1/photos/duplicates` | 获取重复/相似照片组 | S2 |
| GET | `/api/v1/photos/{id}/stream` | 视频流式传输 | S1 |

#### 标签与相册

| 方法 | 路径 | 说明 | 实现阶段 |
|------|------|------|---------|
| GET | `/api/v1/tags` | 获取标签列表（含照片数量） | S2 |
| GET | `/api/v1/tags/{id}/photos` | 获取标签下的照片 | S2 |
| POST | `/api/v1/photos/{id}/tags` | 为照片添加标签 | S2 |
| DELETE | `/api/v1/photos/{id}/tags/{tag_id}` | 移除照片标签 | S2 |
| GET | `/api/v1/albums` | 获取相册列表 | S2 |
| POST | `/api/v1/albums` | 创建相册 | S2 |
| PUT | `/api/v1/albums/{id}` | 更新相册 | S2 |
| DELETE | `/api/v1/albums/{id}` | 删除相册 | S2 |
| POST | `/api/v1/albums/{id}/photos` | 向相册添加照片 | S2 |
| DELETE | `/api/v1/albums/{id}/photos/{photo_id}` | 从相册移除照片 | S2 |

#### 人脸聚类

| 方法 | 路径 | 说明 | 实现阶段 |
|------|------|------|---------|
| GET | `/api/v1/faces/clusters` | 获取人脸簇列表 | S3 |
| GET | `/api/v1/faces/clusters/{id}` | 获取人脸簇详情 | S3 |
| GET | `/api/v1/faces/clusters/{id}/photos` | 获取人脸簇下的照片 | S3 |
| PUT | `/api/v1/faces/clusters/{id}` | 更新人脸簇名称 | S3 |
| POST | `/api/v1/faces/clusters/merge` | 合并多个人脸簇 | S3 |
| POST | `/api/v1/faces/clusters/{id}/split` | 拆分人脸簇 | S3 |
| POST | `/api/v1/faces/detect` | 触发人脸检测任务 | S3 |

#### 系统与任务

| 方法 | 路径 | 说明 | 实现阶段 |
|------|------|------|---------|
| GET | `/api/v1/system/status` | 获取系统状态 | S1 |
| GET | `/api/v1/tasks` | 获取任务列表 | S1 |
| GET | `/api/v1/tasks/{id}` | 获取任务详情 | S1 |
| POST | `/api/v1/tasks/{id}/cancel` | 取消任务 | S1 |
| GET | `/api/v1/system/config` | 获取系统配置 | S1 |
| PUT | `/api/v1/system/config` | 更新系统配置 | S1 |

---

## 5. 视觉设计规范（设计令牌）

> **说明**：本节定义全局 CSS 变量和设计令牌。S0 阶段需在前端项目中创建 `styles/design-tokens.css` 文件，建立完整的设计令牌体系。后续各阶段实现页面时，统一使用这些令牌，确保视觉一致性。

### 5.1 核心风格

以苹果设计语言为基础，融合 visionOS 的半透明质感与空间感。核心词：**留白、毛玻璃、悬浮感、微光影**。

### 5.2 CSS 变量定义

```css
:root {
  /* === 浅色模式 === */
  --bg-primary: #FFFFFF;
  --bg-secondary: #F8F8F8;
  --bg-card: rgba(255, 255, 255, 0.7);
  --bg-card-hover: rgba(255, 255, 255, 0.85);
  --border-color: #F0F0F0;
  --border-color-hover: #E0E0E0;
  --text-primary: #1A1A1A;
  --text-secondary: #666666;
  --text-tertiary: #999999;
  --accent: #7EC8C8;            /* 蒂芙尼蓝/浅青色 */
  --accent-hover: #6BB8B8;
  --accent-light: rgba(126, 200, 200, 0.1);
  --shadow-card: 0 2px 8px rgba(0, 0, 0, 0.06);
  --shadow-card-hover: 0 8px 24px rgba(0, 0, 0, 0.12);
  --shadow-float: 0 16px 48px rgba(0, 0, 0, 0.16);
  --blur-amount: blur(40px);
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 20px;
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-2xl: 48px;
  --font-family: "PingFang SC", "Microsoft YaHei", "SF Pro Display", "Helvetica Neue", sans-serif;
  --font-size-xs: 12px;
  --font-size-sm: 13px;
  --font-size-md: 14px;
  --font-size-lg: 16px;
  --font-size-xl: 20px;
  --font-size-2xl: 28px;
  --font-size-3xl: 36px;
  --transition-fast: 0.15s ease;
  --transition-normal: 0.25s ease;
  --transition-slow: 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  --scrollbar-width: 6px;
  --scrollbar-color: rgba(0, 0, 0, 0.2);
  --scrollbar-color-hover: rgba(0, 0, 0, 0.4);
}

[data-theme="dark"] {
  --bg-primary: #1E1E1E;
  --bg-secondary: #252525;
  --bg-card: rgba(30, 30, 30, 0.7);
  --bg-card-hover: rgba(40, 40, 40, 0.85);
  --border-color: #333333;
  --border-color-hover: #444444;
  --text-primary: #E8E8E8;
  --text-secondary: #999999;
  --text-tertiary: #666666;
  --accent: #7EC8C8;
  --accent-hover: #8ED8D8;
  --accent-light: rgba(126, 200, 200, 0.15);
  --shadow-card: 0 2px 8px rgba(0, 0, 0, 0.2);
  --shadow-card-hover: 0 8px 24px rgba(0, 0, 0, 0.35);
  --shadow-float: 0 16px 48px rgba(0, 0, 0, 0.4);
  --scrollbar-color: rgba(255, 255, 255, 0.2);
  --scrollbar-color-hover: rgba(255, 255, 255, 0.4);
}
```

### 5.3 全局滚动条样式

```css
/* 全局隐藏原生滚动条，仅 Hover 时显现极细半透明滚动条 */
::-webkit-scrollbar {
  width: var(--scrollbar-width);
}
::-webkit-scrollbar-track {
  background: transparent;
}
::-webkit-scrollbar-thumb {
  background: transparent;
  border-radius: 3px;
}
:hover::-webkit-scrollbar-thumb {
  background: var(--scrollbar-color);
}
::-webkit-scrollbar-thumb:hover {
  background: var(--scrollbar-color-hover);
}
```

---

## 6. 部署与工程化

### 6.1 启动脚本与自动配置

提供双击可运行的启动脚本，首次运行时自动完成环境配置：

**Windows (`start.bat`)：**

```batch
@echo off
chcp 65001 >nul
echo ========================================
echo   智能照片管理系统 - 启动中...
echo ========================================

python --version >nul 2>&1
if errorlevel 1 (
    echo [错误] 未检测到 Python，请安装 Python 3.10+
    echo 下载地址: https://www.python.org/downloads/
    pause
    exit /b 1
)

if not exist "venv" (
    echo [首次启动] 正在创建虚拟环境...
    python -m venv venv
    call venv\Scripts\activate.bat
    echo [首次启动] 正在安装依赖（可能需要几分钟）...
    pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
) else (
    call venv\Scripts\activate.bat
)

python main.py
```

**macOS (`start.command`)：**

```bash
#!/bin/bash
cd "$(dirname "$0")"

echo "========================================"
echo "  智能照片管理系统 - 启动中..."
echo "========================================"

if ! command -v python3 &> /dev/null; then
    echo "[错误] 未检测到 Python，请安装 Python 3.10+"
    exit 1
fi

if [ ! -d "venv" ]; then
    echo "[首次启动] 正在创建虚拟环境..."
    python3 -m venv venv
    source venv/bin/activate
    echo "[首次启动] 正在安装依赖（可能需要几分钟）..."
    pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
else
    source venv/bin/activate
fi

python main.py
```

### 6.2 前后端统一端口

前端 Vite 构建产物由 FastAPI 托管，统一端口：

```python
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles

app = FastAPI()

# API 路由注册...
# app.include_router(api_router, prefix="/api/v1")

# 前端静态资源托管（必须在所有 API 路由之后挂载）
app.mount("/", StaticFiles(directory="frontend/dist", html=True), name="frontend")
```

### 6.3 模型权重处理

首次启动时检测本地模型目录，若缺失则自动从镜像源下载：

```python
def ensure_model_weights(model_dir: Path):
    """确保模型权重文件存在，缺失则自动下载。"""
    required_models = {
        'yolov8n.pt': 'https://mirror.example.com/models/yolov8n.pt',
        'clip-vit-b-32.pt': 'https://mirror.example.com/models/clip-vit-b-32.pt',
        'insightface_buffalo_l': 'https://mirror.example.com/models/buffalo_l.zip',
    }
    for model_name, url in required_models.items():
        model_path = model_dir / model_name
        if not model_path.exists():
            logger.info(f"正在下载模型: {model_name}...")
            download_with_progress(url, model_path)
```

### 6.4 系统托盘整合

```python
import pystray
from PIL import Image

def create_tray_icon():
    icon_image = Image.open("assets/tray_icon.png")
    menu = pystray.Menu(
        pystray.MenuItem("打开界面", lambda: webbrowser.open("http://localhost:8000")),
        pystray.MenuItem("停止服务", lambda: shutdown_server()),
    )
    icon = pystray.Icon("PhotoManager", icon_image, "照片管理系统", menu)
    icon.run()
```

### 6.5 浏览器自动唤起

```python
import webbrowser, threading

def open_browser_after_startup():
    import time
    time.sleep(1.5)
    webbrowser.open("http://localhost:8000")

threading.Thread(target=open_browser_after_startup, daemon=True).start()
```

---

## 7. 非功能规格摘要

### 7.1 隐私与安全

| 编号 | 规格项 | 要求 |
|------|--------|------|
| NF-001 | 数据存储 | 所有照片、元数据、AI 模型权重仅存储在用户本地文件系统 |
| NF-002 | 网络隔离 | 系统核心功能（浏览、搜索、标签）完全离线可用，不依赖互联网 |
| NF-003 | 原文件保护 | 严禁对原始照片文件进行任何修改、移动或删除（用户主动触发的删除操作除外） |
| NF-004 | 删除保护 | 用户主动删除操作必须二次确认，且调用 `send2trash` 移至系统回收站 |
| NF-005 | 物理删除 | 物理删除操作必须调用系统原生回收站 API（`send2trash`），确保跨平台安全删除与恢复 |

### 7.2 性能要求

| 编号 | 规格项 | 要求 |
|------|--------|------|
| NF-010 | 缩略图加载 | 1000 张缩略图瀑布流首屏加载时间 < 1s |
| NF-011 | 搜索响应 | 结构化搜索响应时间 < 300ms（10 万级照片） |
| NF-012 | 扫描速度 | 目录扫描速度 ≥ 1000 张/分钟 |
| NF-013 | 缩略图生成 | 图片缩略图生成速度 ≥ 50 张/分钟；视频缩略图 ≥ 10 张/分钟 |
| NF-014 | 内存占用 | 常规浏览时内存占用 < 500MB |

### 7.3 可用性与兼容性

| 编号 | 规格项 | 要求 |
|------|--------|------|
| NF-020 | 操作系统 | 支持 Windows 10+、macOS 12+、Ubuntu 20.04+ |
| NF-021 | 浏览器 | 支持 Chrome 90+、Firefox 90+、Safari 15+、Edge 90+ |
| NF-022 | 部署方式 | 支持 Windows 10+ / macOS 12+ 一键启动（双击 start.bat / start.command），前置条件为已安装 Python 3.10+，首次运行自动创建 venv 并安装依赖 |
| NF-024 | 一键启动 | 新用户从下载到首次看到界面，不超过 4 步操作（安装 Python → 下载 → 解压 → 双击启动）；非首次启动仅需双击启动脚本 |

---

## 8. 项目目录结构

> **S0 阶段交付物**：AI 需按照以下目录结构创建完整的项目脚手架。

```
photo-manager/
├── main.py                          # 后端入口（FastAPI app + uvicorn + 托盘 + 浏览器唤起）
├── start.bat                        # Windows 启动脚本
├── start.command                    # macOS 启动脚本
├── requirements.txt                 # Python 依赖
├── config/
│   └── settings.py                  # 全局配置（端口、路径、阈值等）
├── backend/
│   ├── __init__.py
│   ├── app.py                       # FastAPI 应用实例
│   ├── database.py                  # 数据库连接与初始化
│   ├── models/                      # SQLModel 数据模型
│   │   ├── __init__.py
│   │   ├── library_source.py
│   │   ├── photo.py
│   │   ├── tag.py
│   │   ├── photo_tag.py
│   │   ├── album.py
│   │   ├── photo_album.py
│   │   ├── face_cluster.py
│   │   ├── photo_face.py
│   │   └── photo_embedding.py
│   ├── routers/                     # API 路由（S0 创建骨架，后续阶段填充）
│   │   ├── __init__.py
│   │   ├── libraries.py
│   │   ├── photos.py
│   │   ├── tags.py
│   │   ├── albums.py
│   │   ├── faces.py
│   │   └── system.py
│   ├── services/                    # 业务逻辑（后续阶段实现）
│   │   ├── __init__.py
│   │   ├── scanner.py               # S1: 目录扫描
│   │   ├── exif_parser.py           # S1: EXIF 解析
│   │   ├── thumbnail.py             # S1: 缩略图生成
│   │   ├── tag_generator.py         # S2: YOLOv8 标签生成
│   │   ├── search_engine.py         # S2: 混合搜索引擎
│   │   ├── duplicate_detector.py    # S2: 重复检测
│   │   ├── clip_service.py          # S3: CLIP 向量服务
│   │   └── face_service.py          # S3: 人脸检测与聚类
│   ├── tasks/                       # 异步任务
│   │   ├── __init__.py
│   │   └── task_manager.py          # 任务调度管理
│   └── utils/                       # 工具函数
│       ├── __init__.py
│       └── file_utils.py
├── frontend/
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── index.html
│   ├── src/
│   │   ├── main.ts
│   │   ├── App.vue
│   │   ├── router/
│   │   │   └── index.ts
│   │   ├── stores/                  # Pinia 状态管理
│   │   │   ├── photo.ts
│   │   │   ├── library.ts
│   │   │   ├── tag.ts
│   │   │   └── system.ts
│   │   ├── api/                     # Axios API 封装
│   │   │   ├── client.ts
│   │   │   ├── photos.ts
│   │   │   ├── libraries.ts
│   │   │   ├── tags.ts
│   │   │   └── system.ts
│   │   ├── views/                   # 页面组件
│   │   │   ├── TimelineView.vue
│   │   │   ├── FolderView.vue
│   │   │   ├── SearchView.vue
│   │   │   ├── MapView.vue
│   │   │   ├── PeopleView.vue
│   │   │   ├── TagsView.vue
│   │   │   └── SettingsView.vue
│   │   ├── components/              # 通用组件
│   │   │   ├── PhotoGrid.vue
│   │   │   ├── PhotoDetail.vue
│   │   │   ├── ExifPanel.vue
│   │   │   └── SearchBar.vue
│   │   └── styles/
│   │       ├── design-tokens.css    # 设计令牌（S0 创建）
│   │       ├── global.css           # 全局样式
│   │       └── transitions.css      # 过渡动画
│   └── dist/                        # Vite 构建产物（由 FastAPI 托管）
├── models/                          # AI 模型权重（首次启动自动下载）
├── thumbnails/                      # 缩略图存储目录
├── data/                            # SQLite 数据库文件
│   └── photo_manager.db
└── assets/
    └── tray_icon.png                # 系统托盘图标
```

---

## S0 阶段验收标准

完成本文档后，AI 应交付以下成果：

1. **项目目录结构**：按照第 8 节创建完整的项目脚手架
2. **数据库初始化**：所有 9 张数据表（3.2.1-3.2.9）可正确创建，含全部索引
3. **FastAPI 后端骨架**：所有 API 路由（4.6 节）已注册，返回 501 或空数据
4. **Vue 前端骨架**：项目可启动，显示空白布局框架
5. **设计令牌文件**：`design-tokens.css` 包含完整 CSS 变量（第 5 节）
6. **启动脚本**：`start.bat` 和 `start.command` 可正确创建 venv 并启动项目
7. **前后端统一端口**：FastAPI 可托管前端静态资源，访问 `http://localhost:8000` 可看到前端页面
8. **系统托盘**：启动后可在系统托盘看到图标
9. **浏览器自动唤起**：启动后自动打开浏览器
