# 本地部署智能照片管理系统 — S1: 核心浏览 MVP

| 字段 | 内容 |
|------|------|
| **文档编号** | S1（Phase 1: 核心浏览 MVP） |
| **文档版本** | v3.0 |
| **编制日期** | 2026-05-26 |
| **前置条件** | S0（项目脚手架与共享规范）已完成：项目目录结构已创建、数据库 Schema 已初始化、FastAPI 后端骨架和 Vue 前端骨架已搭建、设计令牌已配置、启动脚本可运行 |
| **AI 实现指引** | 本文档定义 Phase 1（核心浏览 MVP）的完整功能规格。AI 应基于 S0 已搭建的脚手架，实现目录扫描、缩略图生成、时间轴浏览、EXIF 展示、文件监控等核心功能，使系统达到"可浏览照片"的最小可用状态 |

---

## 目录

- [1. 本阶段概述](#1-本阶段概述)
- [2. 功能规格](#2-功能规格)
- [3. API 接口规格（本阶段）](#3-api-接口规格本阶段)
- [4. 前端页面与交互规格（本阶段）](#4-前端页面与交互规格本阶段)
- [5. 验收标准](#5-验收标准)

---

## 1. 本阶段概述

### 1.1 目标与范围

本阶段实现系统的核心浏览功能，使用户能够：

1. 添加本地照片目录作为图库源
2. 自动扫描目录并提取照片元数据
3. 生成缩略图用于快速浏览
4. 按时间轴层级浏览照片
5. 查看照片的 EXIF 详细信息
6. 检测文件系统变动并保持索引一致性

**本阶段不包含**：AI 标签、搜索、相册、去重、地图、人脸聚类等功能（这些在 S2-S4 中实现）。

### 1.2 已有基础设施（S0 交付）

- 完整项目目录结构
- 全部 9 张数据库表已创建
- FastAPI 后端骨架（所有路由返回 501）
- Vue 前端骨架（空白布局）
- 设计令牌 CSS 变量
- 启动脚本（start.bat / start.command）
- 前后端统一端口（FastAPI 托管前端静态资源）
- 系统托盘 + 浏览器自动唤起

### 1.3 本阶段交付物

| 交付物 | 说明 |
|--------|------|
| 目录扫描服务 | 递归扫描图库源目录，提取文件信息并写入数据库 |
| EXIF 解析服务 | 解析照片 EXIF 元数据（时间、相机参数、GPS 等） |
| 缩略图生成服务 | 异步生成多尺寸缩略图（含视频抽帧） |
| 时间轴浏览 | 前端按年/月/日层级展示照片 |
| 文件夹视图 | 按原始目录结构浏览照片 |
| EXIF 详情面板 | 查看照片的完整 EXIF 信息 |
| 文件一致性校验 | 检测文件丢失/新增，保持索引与文件系统同步 |
| 任务管理 | 后台异步任务的状态追踪与进度通知 |

---

## 2. 功能规格

### 2.1 F-001 非破坏性目录接入

| 属性 | 说明 |
|------|------|
| **功能编号** | F-001 |
| **优先级** | P0 (MVP) |
| **描述** | 用户添加本地照片目录作为"图库源"，系统扫描并索引目录中的照片文件，但绝不移动、修改或删除原始文件 |
| **扫描策略** | - **全量扫描**：首次添加图库源时，递归遍历目录下所有支持格式的文件\u003cbr\u003e- **增量扫描**：后续扫描仅处理新增/修改的文件（通过 `file_path` + `file_modified_time` 判断），跳过已索引文件\u003cbr\u003e- **支持的图片格式**：JPEG、PNG、HEIC/HEIF、BMP、TIFF、WebP\u003cbr\u003e- **支持的视频格式**：MP4、MOV、AVI |
| **验收标准** | 1. 用户可添加多个图库源，每个图库源指定一个本地目录路径\u003cbr\u003e2. 扫描过程不修改、不移动、不删除任何原始文件\u003cbr\u003e3. 扫描结果写入数据库，文件路径唯一（`file_path` UNIQUE 约束）\u003cbr\u003e4. 增量扫描正确识别新增文件，不重复索引已有文件\u003cbr\u003e5. 扫描进度通过 WebSocket 实时推送到前端\u003cbr\u003e6. 扫描过程中用户可正常浏览已索引的照片\u003cbr\u003e7. 不支持的文件格式静默跳过，不报错\u003cbr\u003e8. 扫描完成后更新 `LibrarySource.photo_count` 和 `LibrarySource.last_scan_at` |

### 2.2 F-002 照片时间轴

| 属性 | 说明 |
|------|------|
| **功能编号** | F-002 |
| **优先级** | P0 (MVP) |
| **描述** | 按拍摄时间将照片组织为年→月→日三级层级的时间轴，支持展开/折叠浏览 |
| **时间解析优先级** | 1. EXIF `DateTimeOriginal`（原始拍摄时间）\u003cbr\u003e2. EXIF `DateTime`（修改时间）\u003cbr\u003e3. 文件修改时间（`mtime`）\u003cbr\u003e4. 文件夹名解析（如 `2023-10-25` → 2023年10月25日） |
| **验收标准** | 1. 时间轴按年→月→日三级层级展示，支持展开/折叠\u003cbr\u003e2. 无 EXIF 信息的照片能正确回退到文件修改时间或文件夹名\u003cbr\u003e3. 时间轴头部显示每个时间分组的照片数量\u003cbr\u003e4. 支持按时间范围筛选（如"2023年全年"、"2023年6月"） |

### 2.3 F-003 文件夹视图与聚合视图

| 属性 | 说明 |
|------|------|
| **功能编号** | F-003 |
| **优先级** | P0 (MVP) |
| **描述** | 保留原有"按天查看"入口，同时提供跨文件夹的"全部照片"聚合视图 |
| **验收标准** | 1. 文件夹视图保持原始目录结构，每个文件夹显示封面图和照片数量\u003cbr\u003e2. 聚合视图将所有图库源的照片合并展示，按时间排序\u003cbr\u003e3. 两种视图可一键切换，切换时保持当前浏览位置不丢失\u003cbr\u003e4. 聚合视图支持按图库源过滤 |

### 2.4 F-004 缩略图生成

| 属性 | 说明 |
|------|------|
| **功能编号** | F-004 |
| **优先级** | P0 (MVP) |
| **描述** | 后台异步生成多尺寸缩略图，Web 端只加载缩略图，点击才加载原图 |
| **缩略图规格** | - **小缩略图**：长边 300px，短边等比缩放自适应（如横图 300×200，竖图 200×300），用于网格列表\u003cbr\u003e- **中预览图**：长边 1200px，短边等比缩放，用于详情预览\u003cbr\u003e- **大图**：原图，用于全屏查看和下载 |
| **视频缩略图策略** | 对于视频文件（`is_video=True`），使用 ffmpeg 抽取视频总时长 1/4 处的帧作为封面缩略图；若抽帧失败，回退到第 1 秒；若仍失败，使用视频图标占位 |
| **验收标准** | 1. 新增照片后，缩略图在后台异步生成，不阻塞前端操作\u003cbr\u003e2. 缩略图生成进度可在系统任务面板中查看\u003cbr\u003e3. 缩略图存储在独立目录中，不影响原文件\u003cbr\u003e4. 支持 HEIC 格式的缩略图转换\u003cbr\u003e5. 缩略图生成失败时（如文件损坏），显示占位图并标记异常\u003cbr\u003e6. 缩略图尺寸信息（`thumbnail_width`、`thumbnail_height`）写入数据库，前端据此计算占位区域，避免瀑布流布局抖动\u003cbr\u003e7. 视频缩略图生成时，前端展示"播放图标"和"时长标签"覆盖层 |

### 2.5 F-005 EXIF 元数据展示

| 属性 | 说明 |
|------|------|
| **功能编号** | F-005 |
| **优先级** | P0 (MVP) |
| **描述** | 查看照片时展示核心 EXIF 参数 |
| **展示字段** | 相机厂商（Make）、相机型号（Model）、镜头型号（LensModel）、光圈（FNumber）、快门速度（ExposureTime）、ISO 感光度、焦距（FocalLength）、GPS 经纬度、拍摄时间、图片尺寸（宽×高）、文件大小 |
| **验收标准** | 1. 照片详情面板完整展示上述字段，无数据的字段显示"未知"\u003cbr\u003e2. GPS 数据可点击跳转至地图视图（地图视图在 S2 实现，此处仅预留跳转入口）\u003cbr\u003e3. 支持一键复制 EXIF 信息 |

### 2.6 F-014 文件系统监控与一致性校验

| 属性 | 说明 |
|------|------|
| **功能编号** | F-014 |
| **优先级** | P1 |
| **描述** | 用户的文件系统是动态的，系统需感知图库源目录的变更（文件新增、删除、移动、重命名），保持索引与物理文件的一致性 |
| **一致性校验** | - **文件丢失检测**：扫描时检查数据库中已索引的文件是否仍存在于磁盘，若不存在则标记 `file_missing=True`\u003cbr\u003e- **新增文件检测**：扫描时发现磁盘上的新文件不在数据库中，自动索引\u003cbr\u003e- **手动触发**：用户可在图库源上点击"一致性校验"手动触发\u003cbr\u003e- **可选自动监控**：通过 Watchdog 库实时监控目录变动（默认关闭，用户可在设置中开启） |
| **验收标准** | 1. 一致性校验能正确识别丢失文件并标记 `file_missing=True`\u003cbr\u003e2. 一致性校验能发现新增文件并自动索引\u003cbr\u003e3. 丢失文件在前端有明确的视觉标识（如灰色遮罩 + "文件丢失"标签）\u003cbr\u003e4. 用户可手动触发一致性校验\u003cbr\u003e5. Watchdog 监控默认关闭，用户可按需开启 |

---

## 3. API 接口规格（本阶段）

### 3.1 图库源管理

**GET `/api/v1/libraries`** — 获取图库源列表

响应体：

```json
{
  "items": [
    {
      "id": 1,
      "name": "我的照片",
      "path": "/Users/user/Photos",
      "type": "local",
      "scan_status": "completed",
      "last_scan_at": "2026-05-25T10:30:00",
      "photo_count": 1234,
      "created_at": "2026-05-25T10:00:00"
    }
  ]
}
```

**POST `/api/v1/libraries`** — 添加图库源

请求体：

```json
{
  "name": "我的照片",
  "path": "/Users/user/Photos"
}
```

**POST `/api/v1/libraries/{id}/scan`** — 触发目录扫描

响应体：

```json
{
  "task_id": "scan_20260525_001",
  "status": "running",
  "message": "扫描已启动"
}
```

**POST `/api/v1/libraries/{id}/check-consistency`** — 触发一致性校验

**DELETE `/api/v1/libraries/{id}`** — 删除图库源（仅删除数据库记录，不删除原文件）

### 3.2 照片浏览

**GET `/api/v1/photos`** — 获取照片列表（分页）

查询参数：

| 参数 | 类型 | 说明 |
|------|------|------|
| page | int | 页码（默认 1） |
| page_size | int | 每页数量（默认 50，最大 200） |
| library_id | int | 按图库源筛选 |
| year | int | 按年筛选 |
| month | int | 按月筛选（需配合 year） |
| day | int | 按日筛选（需配合 year + month） |
| folder | string | 按文件夹路径筛选 |
| sort_by | string | 排序字段：date_taken（默认）/ file_name / file_size |
| sort_order | string | 排序方向：desc（默认）/ asc |

响应体：

```json
{
  "items": [
    {
      "id": 1,
      "file_name": "IMG_20231025_001.jpg",
      "file_path": "/Users/user/Photos/2023-10-25/IMG_20231025_001.jpg",
      "file_size": 5242880,
      "is_video": false,
      "date_taken": "2023-10-25T14:30:00",
      "thumbnail_path": "/thumbnails/1_small.jpg",
      "thumbnail_width": 300,
      "thumbnail_height": 200,
      "width": 4032,
      "height": 3024,
      "rating": 0,
      "is_favorite": false,
      "file_missing": false
    }
  ],
  "total": 1234,
  "page": 1,
  "page_size": 50
}
```

**GET `/api/v1/photos/{id}`** — 获取照片详情

**GET `/api/v1/photos/{id}/exif`** — 获取照片 EXIF 信息

响应体：

```json
{
  "photo_id": 1,
  "camera_make": "Apple",
  "camera_model": "iPhone 15 Pro",
  "lens_model": "iPhone 15 Pro back triple camera 6.765mm f/1.78",
  "f_number": 1.78,
  "exposure_time": "1/120",
  "iso": 64,
  "focal_length": 6.77,
  "gps_latitude": 31.2304,
  "gps_longitude": 121.4737,
  "date_taken": "2023-10-25T14:30:00",
  "width": 4032,
  "height": 3024,
  "file_size": 5242880,
  "orientation": 1
}
```

**PUT `/api/v1/photos/{id}`** — 更新照片元数据（评分、收藏等）

请求体：

```json
{
  "rating": 4,
  "is_favorite": true
}
```

**GET `/api/v1/photos/{id}/stream`** — 视频流式传输（仅视频文件）

### 3.3 系统与任务

**GET `/api/v1/system/status`** — 获取系统状态

响应体：

```json
{
  "total_photos": 12345,
  "total_libraries": 3,
  "db_size_mb": 45.6,
  "thumbnail_size_mb": 234.5,
  "is_scanning": false,
  "active_tasks": 0
}
```

**GET `/api/v1/tasks`** — 获取任务列表

**GET `/api/v1/tasks/{id}`** — 获取任务详情

**POST `/api/v1/tasks/{id}/cancel`** — 取消任务

**GET `/api/v1/system/config`** — 获取系统配置

**PUT `/api/v1/system/config`** — 更新系统配置

---

## 4. 前端页面与交互规格（本阶段）

### 4.1 页面清单（本阶段）

| 编号 | 页面名称 | 路由 | 优先级 | 说明 |
|------|---------|------|--------|------|
| P-001 | 时间轴首页 | `/` | P0 | 默认首页，按年/月/日层级展示照片 |
| P-002 | 文件夹视图 | `/folders` | P0 | 按原始目录结构浏览 |
| P-003 | 照片详情 | `/photos/:id` | P0 | 照片大图预览 + EXIF 信息 |
| P-008 | 设置页 | `/settings` | P1 | 图库源管理、系统配置 |

### 4.2 关键交互流程

#### 4.2.1 添加图库源与扫描

```
首页 → 点击"添加图库源" → 弹出对话框（输入名称 + 选择目录路径）→ 确认
  → 后端创建 LibrarySource 记录 → 自动触发全量扫描
  → 前端通过 WebSocket 接收扫描进度（已扫描 X 张 / 共 Y 个文件）
  → 扫描完成后自动跳转至时间轴首页，展示新扫描的照片
```

#### 4.2.2 时间轴浏览

```
时间轴首页 → 显示年份列表（2024 / 2023 / 2022...），每个年份显示照片数量
  → 点击年份 → 展开月份列表（12月 / 11月 / ...），每个月份显示照片数量
  → 点击月份 → 展开日期列表，每个日期显示封面缩略图
  → 点击日期 → 瀑布流展示该日期的所有照片缩略图
  → 点击照片 → 进入照片详情页
```

#### 4.2.3 照片详情与 EXIF 查看

```
瀑布流 → 点击照片 → 进入照片详情页（全屏大图预览）
  → 右侧抽屉面板展示 EXIF 信息
  → 支持左右箭头切换上/下一张
  → 支持手势缩放（PhotoSwipe）
  → 支持评分和收藏操作
```

### 4.3 瀑布流虚拟滚动规格

- 使用 `vue-virtual-scroller` 实现网格布局虚拟滚动
- 列数根据容器宽度自适应（最小列宽 200px，最大 300px）
- 缩略图使用等比缩放（长边 300px），预计算占位区域避免布局抖动
- 1 万张照片下流畅滚动，无明显卡顿
- 滚动时懒加载缩略图，离开可视区域的缩略图释放 DOM

---

## 5. 验收标准

**MVP（Phase 1）验收必须满足：**

1. 可添加图库源并完成目录扫描，原文件无任何变动
2. 时间轴按年/月/日层级正确展示照片
3. 缩略图异步生成（含视频抽帧），前端加载流畅
4. EXIF 元数据正确解析和展示
5. 虚拟滚动在 1 万张照片下流畅运行，瀑布流无布局抖动
6. 文件夹视图和聚合视图可正常切换
7. 提供 Windows/macOS 一键启动脚本（start.bat / start.command），双击即可启动并自动打开浏览器，首次运行自动配置环境；物理删除操作正确移入系统回收站
8. 照片方向根据 EXIF Orientation 正确显示
9. 文件一致性校验能正确标记丢失文件和发现新增文件
10. 扫描和缩略图生成进度通过 WebSocket 实时推送到前端
