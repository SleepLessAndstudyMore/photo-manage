# 本地部署智能照片管理系统 — S3: AI 语义搜索与人脸聚类

| 字段 | 内容 |
|------|------|
| **文档编号** | S3（Phase 3: AI 语义搜索与人脸聚类） |
| **文档版本** | v3.0 |
| **编制日期** | 2026-05-26 |
| **前置条件** | S0 + S1 + S2 已完成：核心浏览功能已实现、YOLOv8 标签/相册/搜索/去重/地图已实现 |
| **AI 实现指引** | 本文档定义 Phase 3 的完整功能规格。AI 应基于 S2 已实现的智能整理功能，新增 CLIP 语义搜索（补全混合搜索的语义部分）和 InsightFace 人脸聚类功能 |

---

## 目录

- [1. 本阶段概述](#1-本阶段概述)
- [2. 功能规格](#2-功能规格)
- [3. API 接口规格（本阶段）](#3-api-接口规格本阶段)
- [4. 前端页面与交互规格（本阶段）](#4-前端页面与交互规格本阶段)
- [5. 验收标准](#5-验收标准)

---

## 1. 本阶段概述

### 1.1 目标与范围

本阶段在智能整理与检索基础上，实现 AI 赋能功能：

1. **CLIP 语义搜索**：将照片和搜索文本映射到同一向量空间，实现跨模态语义检索，补全 S2 混合搜索的语义部分
2. **InsightFace 人脸聚类**：检测照片中的人脸，自动聚类为同一"人物"，支持命名和管理

### 1.2 已有基础设施（S0 + S1 + S2 交付）

- 完整数据库 Schema（PhotoEmbedding、PhotoFace、FaceCluster 表已创建但未使用）
- 混合搜索框架已实现（结构化搜索部分工作正常，语义部分返回空结果）
- YOLOv8 标签生成服务已实现（后台异步任务机制可复用）
- WebSocket 通知机制已实现

### 1.3 本阶段交付物

| 交付物 | 说明 |
|--------|------|
| CLIP 向量生成服务 | 对每张照片生成 512 维 CLIP 向量，存入 PhotoEmbedding 表 |
| CLIP 语义搜索 | 将搜索文本编码为向量，在 PhotoEmbedding 中进行 ANN 搜索 |
| 混合搜索补全 | 将 CLIP 语义搜索与 S2 的结构化搜索整合，实现完整混合搜索 |
| 人脸检测服务 | 检测照片中的人脸，提取边界框和特征向量 |
| 人脸聚类服务 | 基于特征向量将相似人脸聚类为同一"人物" |
| 人物管理 | 为人物命名、合并/拆分聚类 |

---

## 2. 功能规格

### 2.1 CLIP 语义搜索

| 属性 | 说明 |
|------|------|
| **功能编号** | F-009 扩展 |
| **优先级** | P1 |
| **描述** | 使用 CLIP (ViT-B/32) 模型将照片和搜索文本映射到同一 512 维向量空间，实现跨模态语义检索 |
| **向量生成策略** | - **全量生成**：首次启用时，对数据库中所有照片生成 CLIP 向量\u003cbr\u003e- **增量生成**：新扫描的照片在后台自动生成向量\u003cbr\u003e- **异步执行**：向量生成在后台异步进行，不阻塞用户操作 |
| **搜索流程** | 1. 用户输入自然语言查询\u003cbr\u003e2. 后端 NLP/正则提取时间/结构化实体作为 Filter\u003cbr\u003e3. 语义部分通过 CLIP 文本编码器生成 512 维向量\u003cbr\u003e4. 在 PhotoEmbedding 表中进行 ANN 搜索\u003cbr\u003e5. 结果与 Filter 取交集\u003cbr\u003e6. 按相似度排序返回 |
| **验收标准** | 1. CLIP 向量生成在后台异步进行，进度可通过任务面板查看\u003cbr\u003e2. 语义搜索返回语义相关的结果（如搜索"海边"能找到海滩照片，即使没有"海边"标签）\u003cbr\u003e3. 混合搜索正确整合结构化过滤和语义匹配\u003cbr\u003e4. 搜索响应时间 < 600ms（10 万级照片索引）\u003cbr\u003e5. 向量数据存储在独立的 PhotoEmbedding 表中，不影响主表性能 |

**AI 职责解耦说明：**

```
用户上传/扫描照片
       │
       ├──→ YOLOv8 推理 ──→ 输出物体标签（cat, dog, car...）──→ 写入 Tag 表
       │                                                         type='auto'
       │
       └──→ CLIP 推理 ──→ 输出 512维向量 ──→ 写入 PhotoEmbedding 表
                                                （用于语义搜索，不生成标签）

用户搜索"去年在海边的猫"
       │
       ├──→ NLP/正则提取 ──→ 时间实体: "去年" → date_from/date_to
       │                     结构化实体: "猫" → 通过 name_zh 字段映射至 tag_id
       │
       └──→ CLIP 文本编码 ──→ "海边" → 512维向量 ──→ 向量检索
                                                   │
                              Filter: date_range + tag_id
                                                   │
                              结果 = 向量检索 ∩ Filter
```

### 2.2 F-008 人脸聚类

| 属性 | 说明 |
|------|------|
| **功能编号** | F-008 |
| **优先级** | P2 |
| **描述** | 检测并提取人脸特征，将同一人的照片自动聚类 |
| **人脸检测** | 使用 InsightFace (buffalo_l) 模型检测照片中的人脸，输出：边界框（bbox）、特征向量（512维 float32）、检测置信度 |
| **人脸聚类** | 基于特征向量将相似人脸聚类为同一"人物"，使用 DBSCAN 或层次聚类算法 |
| **验收标准** | 1. 系统自动检测照片中的人脸，提取人脸区域和特征向量\u003cbr\u003e2. 基于特征向量将相似人脸聚类为同一"人物"\u003cbr\u003e3. 用户可为每个人物命名，也可手动合并/拆分聚类\u003cbr\u003e4. 人物页面展示每个人物的代表照片和照片数量\u003cbr\u003e5. 点击人物可查看该人物的所有照片\u003cbr\u003e6. 人脸检测和聚类过程完全在本地完成，不上传任何数据\u003cbr\u003e7. 人脸聚类准确率 ≥ 85% |

---

## 3. API 接口规格（本阶段）

### 3.1 语义搜索

**POST `/api/v1/photos/search`** — 混合搜索（补全语义部分）

S2 阶段已实现结构化搜索，本阶段补全 CLIP 语义搜索。混合搜索请求体不变：

```json
{
  "mode": "hybrid",
  "query": "去年在海边的猫",
  "filters": {},
  "page": 1,
  "page_size": 50
}
```

后端处理流程变更：
1. NLP/正则提取 → 时间实体 + 标签映射（S2 已实现）
2. **CLIP 文本编码 → 512 维向量**（本阶段新增）
3. **PhotoEmbedding ANN 搜索**（本阶段新增）
4. Filter 叠加 → 结果 = 向量检索 ∩ Filter
5. 按相似度排序返回

响应体新增 `similarity_score` 字段：

```json
{
  "items": [
    {
      "id": 42,
      "file_name": "IMG_20230815_beach.jpg",
      "similarity_score": 0.87,
      "date_taken": "2023-08-15T16:30:00",
      "thumbnail_path": "/thumbnails/42_small.jpg"
    }
  ],
  "total": 15,
  "page": 1,
  "page_size": 50
}
```

### 3.2 人脸聚类

**GET `/api/v1/faces/clusters`** — 获取人脸簇列表

**GET `/api/v1/faces/clusters/{id}`** — 获取人脸簇详情

**GET `/api/v1/faces/clusters/{id}/photos`** — 获取人脸簇下的照片

**PUT `/api/v1/faces/clusters/{id}`** — 更新人脸簇名称

请求体：

```json
{
  "name": "张三"
}
```

**POST `/api/v1/faces/clusters/merge`** — 合并多个人脸簇

请求体：

```json
{
  "cluster_ids": [1, 3, 7]
}
```

**POST `/api/v1/faces/clusters/{id}/split`** — 拆分人脸簇

**POST `/api/v1/faces/detect`** — 触发人脸检测任务

---

## 4. 前端页面与交互规格（本阶段）

### 4.1 新增页面

| 编号 | 页面名称 | 路由 | 优先级 | 说明 |
|------|---------|------|--------|------|
| P-010 | 人物页 | `/people` | P2 | 展示所有人物及代表照片 |
| P-011 | 人物详情 | `/people/:id` | P2 | 展示某人物的所有照片 |

### 4.2 关键交互流程

#### 4.2.1 人物浏览与管理

```
人物页 → 展示所有人物卡片（代表照片 + 名称 + 照片数量）
  → 未命名人物显示"人物 1"、"人物 2"等默认名称
  → 点击人物 → 进入人物详情页，展示该人物的所有照片
  → 用户可为人物命名
  → 用户可合并多个人物（长按选择 → 合并）
  → 用户可拆分人物（进入详情 → 选择人脸 → 拆分为新人物）
```

#### 4.2.2 语义搜索体验

```
搜索页 → 输入"海边日落"
  → 后端 CLIP 编码 → 向量检索 → 返回语义相关照片
  → 搜索结果按相似度排序，显示相似度分数
  → 用户可对结果二次过滤（如添加时间范围）
```

---

## 5. 验收标准

**Phase 3 验收追加（在 Phase 2 基础上）：**

1. CLIP 自然语言搜索返回语义相关结果（仅语义部分，时间部分由结构化过滤处理）
2. 混合搜索正确整合结构化过滤和 CLIP 语义匹配
3. 人脸聚类准确率 ≥ 85%
4. 人物页面展示所有人物，支持命名、合并、拆分
5. CLIP 向量生成在后台异步进行，不阻塞用户操作
6. 搜索响应时间 < 600ms（10 万级照片索引）
