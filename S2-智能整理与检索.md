# 本地部署智能照片管理系统 — S2: 智能整理与检索

| 字段 | 内容 |
|------|------|
| **文档编号** | S2（Phase 2: 智能整理与检索） |
| **文档版本** | v3.0 |
| **编制日期** | 2026-05-26 |
| **前置条件** | S0 + S1 已完成：项目脚手架已搭建、目录扫描/缩略图/时间轴/EXIF/文件监控等核心浏览功能已实现 |
| **AI 实现指引** | 本文档定义 Phase 2 的完整功能规格。AI 应基于 S1 已实现的核心浏览功能，新增 YOLOv8 自动标签、手动标签与相册、高级搜索（含混合搜索）、重复照片清理、GPS 地图视图等功能 |

---

## 目录

- [1. 本阶段概述](#1-本阶段概述)
- [2. 功能规格](#2-功能规格)
- [3. API 接口规格（本阶段）](#3-api-接口规格本阶段)
- [4. 前端页面与交互规格（本阶段）](#4-前端页面与交互规格本阶段)
- [5. 验收标准](#5-验收标准)

---

## 1. 本阶段概述

### 1.1 目标与范围

本阶段在核心浏览 MVP 基础上，实现智能整理与检索功能：

1. YOLOv8 自动识别照片中的物体并生成标签（含中英文映射）
2. 手动标签与相册管理
3. 高级组合搜索 + 自然语言混合搜索
4. 重复/相似照片检测与清理
5. GPS 地图视图

**本阶段不包含**：CLIP 语义搜索、人脸聚类（S3 实现）。

### 1.2 已有基础设施（S0 + S1 交付）

- 完整数据库 Schema（Tag、PhotoTag、Album、PhotoAlbum 表已创建但未使用）
- 目录扫描、缩略图生成、时间轴浏览、EXIF 展示已实现
- 照片列表 API、图库源管理 API 已实现
- 前端时间轴首页、文件夹视图、照片详情页已实现
- WebSocket 通知机制已实现

### 1.3 本阶段交付物

| 交付物 | 说明 |
|--------|------|
| YOLOv8 标签生成服务 | 自动识别照片物体，生成 COCO 80 类标签（含置信度） |
| 中英文标签映射 | Tag 表 name_zh 字段填充，COCO 80 类内置中英文映射表 |
| 标签管理 | 查看、删除、修改 AI 标签；手动添加标签 |
| 相册管理 | 创建、重命名、删除相册；添加/移除照片 |
| 高级搜索 | 结构化组合搜索 + 自然语言混合搜索 |
| 重复照片检测 | 区分比特级重复和视觉级相似，提供分层清理策略 |
| GPS 地图视图 | 在地图上展示有 GPS 信息的照片 |

---

## 2. 功能规格

### 2.1 F-007 智能相册/标签系统

| 属性 | 说明 |
|------|------|
| **功能编号** | F-007 |
| **优先级** | P1 |
| **描述** | 基于本地 AI 模型自动打标，并支持手动创建相册 |
| **标签类型** | - **场景标签（auto）**：由 YOLOv8 目标检测模型自动识别生成，输出 COCO 80 类物体标签（如 person、car、dog、cat、bicycle 等），置信度低于阈值的标签自动丢弃\u003cbr\u003e- **手动标签（manual）**：用户自定义添加的标签\u003cbr\u003e- **人脸标签（face）**：由人脸聚类自动生成（S3 实现） |
| **AI 标签生成机制** | YOLOv8 负责具体物体标签的生成（输出结构化类别名称 + 置信度），CLIP **不参与标签生成**，仅用于自然语言语义搜索。两者职责完全解耦 |
| **中英文标签映射（v3.0 新增）** | YOLOv8 COCO 80 类输出英文标签（如 `cat`、`dog`、`person`），而中文用户搜索时输入中文（如"猫"、"狗"、"人"）。系统需维护一套中英文标签映射机制：1) Tag 表通过 `name`（英文）+ `name_zh`（中文）双字段存储双语标签；2) MVP 阶段内置 COCO 80 类的完整中英文映射表；3) 搜索时对中文关键词优先匹配 `name_zh` 字段，同时兼容英文 `name` 字段匹配；4) 后续自定义标签（非 COCO 类别）由用户在创建时填写中英文名称 |
| **验收标准** | 1. AI 自动标签在后台异步生成，不阻塞前端操作\u003cbr\u003e2. 用户可查看、删除、修改 AI 生成的标签\u003cbr\u003e3. 手动相册支持创建、重命名、删除，可将不同日期的照片加入同一相册\u003cbr\u003e4. 相册支持自定义封面和排序\u003cbr\u003e5. 标签云页面展示所有标签及其对应照片数量\u003cbr\u003e6. YOLOv8 标签附带置信度（存储在 PhotoTag 表），用户可设置置信度阈值过滤低质量标签\u003cbr\u003e7. 中文搜索"猫"能正确命中 Tag.name_zh="猫"对应的照片 |

### 2.2 F-009 高级搜索

| 属性 | 说明 |
|------|------|
| **功能编号** | F-009 |
| **优先级** | P1 |
| **描述** | 支持组合条件搜索和自然语言搜索，采用"混合搜索"策略 |
| **搜索模式** | - **结构化搜索**：支持多条件组合，如 `相机=Sony AND 时间>2023 AND 标签=猫`\u003cbr\u003e- **自然语言搜索（混合策略）**：如"去年在海边拍的照片"，后端先通过轻量级 NLP/正则提取时间实体（"去年" → 具体的 `date_from` 和 `date_to`）、相机实体等结构化数据作为 Filter，然后将剩余的语义部分（"海边"）转为 CLIP 向量进行语义匹配。**CLIP 仅处理语义部分，不处理时间/结构化概念** |
| **可搜索字段** | 文件名、拍摄时间、相机型号、镜头、光圈、快门、ISO、GPS 位置、标签、人物、相册、评分、收藏状态 |
| **验收标准** | 1. 结构化搜索支持 AND/OR/NOT 逻辑组合\u003cbr\u003e2. 自然语言搜索采用混合策略：时间实体提取 + CLIP 语义匹配，返回语义相关且时间范围正确的结果\u003cbr\u003e3. 搜索结果支持二次过滤和排序\u003cbr\u003e4. 搜索历史自动保存，支持快速重复搜索\u003cbr\u003e5. 搜索响应时间 < 500ms（10 万级照片索引）\u003cbr\u003e6. 混合搜索的时间实体提取准确率 ≥ 90% |

**混合搜索请求体示例：**

```json
{
  "query": "去年在海边的猫",
  "mode": "hybrid",
  "filters": {},
  "page": 1,
  "page_size": 50
}
```

**后端混合搜索处理流程：**

1. **NLP/正则提取**：从查询中提取时间实体（"去年" → `date_from`/`date_to`）和结构化实体（"猫" → 通过 `name_zh` 映射至 `tag_id`）
2. **CLIP 文本编码**：将语义部分（"海边"）编码为 512 维向量
3. **向量检索**：在 PhotoEmbedding 表中进行 ANN 搜索
4. **Filter 叠加**：`结果 = 向量检索 ∩ Filter(date_range + tag_id)`
5. **排序返回**：按相似度排序，分页返回

> **注意**：CLIP 语义搜索的完整实现在 S3 阶段。本阶段（S2）的混合搜索仅实现结构化搜索部分（时间实体提取 + 标签过滤），语义部分返回空结果。S3 阶段补充 CLIP 向量检索后，混合搜索才能完整工作。

### 2.3 F-010 重复/相似照片清理

| 属性 | 说明 |
|------|------|
| **功能编号** | F-010 |
| **优先级** | P1 |
| **描述** | 区分比特级重复和视觉级相似，提供分层清理策略 |
| **重复类型细分** | - **比特级重复**：`file_hash`（MD5/SHA256）完全一致，表示文件内容完全相同，清理无风险\u003cbr\u003e- **视觉级相似**：`phash` 汉明距离 ≤ 阈值，表示视觉上相似但像素不同（如连拍、HDR），需用户对比确认 |
| **验收标准** | 1. 系统自动识别比特级重复文件组，标记为"完全重复"，提示可安全清理\u003cbr\u003e2. 系统自动识别视觉级相似文件组，标记为"视觉相似"，需用户对比确认\u003cbr\u003e3. 两种重复类型在界面展示和交互提示上有明显区分\u003cbr\u003e4. 相似照片对比界面支持并排/叠加查看\u003cbr\u003e5. 删除操作调用 `send2trash` 移至系统回收站\u003cbr\u003e6. 相似度阈值可由用户自定义调整 |

### 2.4 F-006 GPS 地图视窗

| 属性 | 说明 |
|------|------|
| **功能编号** | F-006 |
| **优先级** | P1 |
| **描述** | 在地图上展示有 GPS 信息的照片，支持按地理位置浏览 |
| **验收标准** | 1. 地图上以聚合点方式展示照片，聚合数量随缩放级别动态变化\u003cbr\u003e2. 点击聚合点可展开为子聚合或显示照片列表\u003cbr\u003e3. 支持离线地图瓦片，断网环境下地图仍可正常使用\u003cbr\u003e4. 地图上的照片标记可点击，弹出照片预览卡片\u003cbr\u003e5. 支持在地图上框选区域，筛选该区域内的所有照片 |

---

## 3. API 接口规格（本阶段）

### 3.1 标签与相册

**GET `/api/v1/tags`** — 获取标签列表（含照片数量）

查询参数：

| 参数 | 类型 | 说明 |
|------|------|------|
| type | string | 按类型筛选：auto / manual / face |
| min_confidence | float | 最低置信度阈值（仅 auto 类型） |
| sort_by | string | 排序：photo_count（默认）/ name |
| page | int | 页码 |
| page_size | int | 每页数量 |

**GET `/api/v1/tags/{id}/photos`** — 获取标签下的照片

**POST `/api/v1/photos/{id}/tags`** — 为照片添加标签

请求体：

```json
{
  "tag_id": 5,
  "source": "manual"
}
```

**DELETE `/api/v1/photos/{id}/tags/{tag_id}`** — 移除照片标签

**GET `/api/v1/albums`** — 获取相册列表

**POST `/api/v1/albums`** — 创建相册

请求体：

```json
{
  "name": "旅行照片",
  "description": "2023年旅行合集"
}
```

**PUT `/api/v1/albums/{id}`** — 更新相册

**DELETE `/api/v1/albums/{id}`** — 删除相册

**POST `/api/v1/albums/{id}/photos`** — 向相册添加照片

请求体：

```json
{
  "photo_ids": [1, 2, 3, 5, 8]
}
```

**DELETE `/api/v1/albums/{id}/photos/{photo_id}`** — 从相册移除照片

### 3.2 照片搜索

**POST `/api/v1/photos/search`** — 搜索照片（结构化 + 混合搜索）

请求体：

```json
{
  "mode": "structured",
  "filters": {
    "date_from": "2023-01-01",
    "date_to": "2023-12-31",
    "camera_model": "Sony A7M4",
    "tag_ids": [5, 12],
    "rating_min": 3,
    "is_favorite": true,
    "library_id": 1
  },
  "logic": "AND",
  "sort_by": "date_taken",
  "sort_order": "desc",
  "page": 1,
  "page_size": 50
}
```

混合搜索请求体：

```json
{
  "mode": "hybrid",
  "query": "去年在海边的猫",
  "filters": {},
  "page": 1,
  "page_size": 50
}
```

### 3.3 重复照片

**GET `/api/v1/photos/duplicates`** — 获取重复/相似照片组

查询参数：

| 参数 | 类型 | 说明 |
|------|------|------|
| type | string | 重复类型：bitwise / visual / all（默认） |
| threshold | int | 视觉相似度阈值（汉明距离），默认 10 |
| page | int | 页码 |
| page_size | int | 每页数量 |

响应体示例：

```json
{
  "groups": [
    {
      "type": "bitwise",
      "hash": "a1b2c3d4e5f6...",
      "photos": [
        {"id": 101, "file_path": "/photos/2025/IMG_001.jpg", "file_size": 5242880},
        {"id": 205, "file_path": "/photos/backup/IMG_001.jpg", "file_size": 5242880}
      ],
      "suggestion": "safe_to_delete",
      "message": "文件内容完全相同，可安全删除冗余副本"
    },
    {
      "type": "visual",
      "distance": 6,
      "photos": [
        {"id": 302, "file_path": "/photos/2025/IMG_002.jpg", "file_size": 5130000},
        {"id": 303, "file_path": "/photos/2025/IMG_003.jpg", "file_size": 5150000}
      ],
      "suggestion": "review_needed",
      "message": "视觉相似照片（可能为连拍），请对比后决定"
    }
  ]
}
```

---

## 4. 前端页面与交互规格（本阶段）

### 4.1 新增页面

| 编号 | 页面名称 | 路由 | 优先级 | 说明 |
|------|---------|------|--------|------|
| P-004 | 标签云 | `/tags` | P1 | 展示所有标签及对应照片数量 |
| P-005 | 搜索页 | `/search` | P1 | 高级搜索与自然语言搜索 |
| P-006 | 地图页 | `/map` | P1 | GPS 照片地图视图 |
| P-007 | 重复照片 | `/duplicates` | P1 | 重复/相似照片清理 |
| P-009 | 相册详情 | `/albums/:id` | P1 | 相册内照片浏览 |

### 4.2 关键交互流程

#### 4.2.1 标签生成与浏览

```
扫描完成 → 后端自动触发 YOLOv8 标签生成（后台异步）
  → 标签生成进度通过 WebSocket 推送
  → 标签云页面实时更新
  → 用户点击标签 → 展示该标签下的所有照片
  → 用户可删除低置信度标签，或设置置信度阈值过滤
```

#### 4.2.2 混合搜索流程

```
搜索页 → 输入自然语言（如"去年在海边的猫"）
  → 后端 NLP/正则提取时间实体 + 标签映射
  → 结构化部分作为 Filter
  → 语义部分编码为 CLIP 向量（S3 阶段补充）
  → 返回搜索结果
  → 用户可对结果二次过滤和排序
```

#### 4.2.3 重复照片清理

```
重复照片页 → 展示重复/相似照片组列表
  → 比特级重复：标记"完全重复"，建议安全删除
  → 视觉级相似：标记"视觉相似"，点击进入并排对比
  → 用户选择保留哪张 → 删除其余 → 调用 send2trash 移至回收站
```

---

## 5. 验收标准

**Phase 2 验收追加（在 Phase 1 基础上）：**

1. 标签和相册功能完整可用，YOLOv8 标签附带置信度（存储在 PhotoTag 表）
2. 高级搜索返回正确结果，混合搜索正确提取时间实体
3. 重复照片清理区分比特级和视觉级，提供不同清理建议
4. 地图视图正确展示 GPS 照片
5. 中文搜索"猫"能正确命中标签为"cat"的照片（通过 name_zh 映射）
6. 标签云页面展示所有标签及对应照片数量
7. 相册支持创建、重命名、删除、添加/移除照片
